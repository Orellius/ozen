{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the dashboard and pill windows",
  "windows": ["main", "pill"],
  "permissions": ["core:default"]
}
